// Constant folding
/*
void main ( void ) {
    gl_FragColor = vec4(vec3(0.8283185307179586, 2, -1.0), 1.0);
    return;
}
 */
function shade(env) {
    return new Vec3((1 + Math.PI) / 5, true + true + false, Math.sin(-Math.PI/2));
}
